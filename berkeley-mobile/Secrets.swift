//
//  Secrets.swift
//  berkeley-mobile
//
//  Placeholder secrets committed so the project compiles from a clean clone
//  (this file is listed as a compiled source in the Xcode project).
//  Replace the values locally with real credentials when needed — never commit real values.
//

enum Secrets {
    static let apiKey = "PLACEHOLDER_API_KEY"
    static let googlePlacesAPIKey = "PLACEHOLDER_GOOGLE_PLACES_API_KEY"
}
